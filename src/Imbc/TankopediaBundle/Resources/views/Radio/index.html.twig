{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Radio list</h1>

    <table class="table table-condensed table-hover table-striped records_list">
        <thead>
            <tr>
                <th>Tier</th>
                <th>Name</th>
                <th>XP required</th>
                <th>Cost</th>
                <th>Weight</th>
                <th>Signal Range</th>
                <th>Compatible With</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.tier }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.xp }}</td>
                <td>{{ entity.cost }}</td>
                <td>{{ entity.weight }}</td>
                <td>{{ entity.signalRange }}</td>
                <td>
                    {% for tank in entity.tanks %}
                        {{ tank }}
                    {% endfor %}
                </td>
                <td>
                    <div class="btn-group">
                        <button class="btn btn-mini">
                            <a href="{{ path('tankopedia_radio_show', { 'id': entity.id }) }}">show</a>
                        </button>
                            <button class="btn btn-mini">
                            <a href="{{ path('tankopedia_radio_edit', { 'id': entity.id }) }}">edit</a>
                        </button>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tankopedia_radio_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
