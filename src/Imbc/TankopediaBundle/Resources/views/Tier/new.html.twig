{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tier creation</h1>

    <form action="{{ path('tankopedia_tier_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tankopedia_tier') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
