{% extends 'ImbcTankopediaBundle::_layout.html.twig' %}

{% block body -%}

{% if edit == true %}
    <h1>Editing existing Class</h1>
{% else %}
    <h1>New Class</h1>
{% endif %}
<form class="form-horizontal" method="POST" action="">
    <div class="form-horizontal">
        <div class="control-group">
            <div class="control-label">
                {{ form_label( class.name ) }}
            </div>
            <div class="controls">
                {{ form_widget( class.name ) }}
            </div>
        </div>
        <div class="control-group">
            <div class="control-label">
                {{ form_label( class.tanks ) }}
            </div>
            <div class="controls">
                {{ form_widget( class.tanks ) }}
            </div>
        </div>
    </div>
    <div class="btn-group">
        <a type="submit" class="btn btn-mini">
            <i class="icon-ok"></i> Save Form
        </a>
    </div>
</form>
{% endblock %}
