{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nationality list</h1>

    <table class="table table-condensed table-hover table-striped records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Abreviation</th>
                <th>Name</th>
                <th>Number of Tanks</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tankopedia_nationality_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.abreviation }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.tanks | length }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tankopedia_nationality_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tankopedia_nationality_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tankopedia_nationality_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
