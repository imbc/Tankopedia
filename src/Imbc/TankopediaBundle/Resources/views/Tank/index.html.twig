{% extends '::base.html.twig' %}

{% block body -%}
<div>
    <h1>Tank list</h1> ({{ tanks | length }} tanks in total on the databse)
</div>
<div class="btn-group pull-right">
    <a class="btn btn-mini" href="{{ path( 'tankopedia_tank_new' ) }}">
        <i class="icon-plus"></i> Add a Tank
    </a>
</div>

{{ grid(grid, 'ImbcTankopediaBundle:Grid:_tank.html.twig') }}
{% endblock %}
