{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tank list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Premium</th>
                <th>Reward</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tankopedia_tank_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.premium }}</td>
                <td>{{ entity.reward }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tankopedia_tank_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tankopedia_tank_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tankopedia_tank_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
